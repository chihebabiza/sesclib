<%- include('../parts/head') %>

    <body>
        <section class="colored-section" id="login">
            <div class="container-fluid min-vh-100 d-flex justify-content-center align-items-center">
                <form class="mx-auto login-form form col-12 col-sm-8 col-md-6 col-lg-4" method="POST" action="/login">
                    <h4 class="form-heading text-center">Log In</h4>

                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                        <% } %>

                            <% if (success) { %>
                                <div class="alert alert-success">
                                    <%= success %>
                                </div>
                                <% } %>

                                    <div class="input-group mb-3">
                                        <span class="input-group-text">
                                            <i class="fa-solid fa-envelope"></i>
                                        </span>
                                        <input type="email" name="email" class="form-control"
                                            placeholder="Email address" required>
                                    </div>

                                    <div class="input-group mb-3">
                                        <span class="input-group-text">
                                            <i class="fa-solid fa-lock"></i>
                                        </span>
                                        <input type="password" name="password" class="form-control"
                                            placeholder="Password" required>
                                    </div>

                                    <button type="submit" class="btn btn-primary login-btn w-100">Login</button>

                                    <p class="form-text text-center mt-3">Don't have an account? <a href="/register"
                                            class="form-link">Sign up</a></p>
                                    <p class="form-text text-center">Want to go back? <a href="/" class="form-link">Back
                                            to Home</a></p>
                </form>
            </div>
        </section>
    </body>

    </html>